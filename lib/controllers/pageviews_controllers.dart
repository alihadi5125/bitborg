import 'package:flutter/material.dart';

class PageViewControllers{
  static PageController mainPageViewController = PageController(
    initialPage : 0,
  );

  static PageController newsPageViewController = PageController(
    initialPage: 0,
  );

  static PageController signalsPageViewController = PageController(
    initialPage: 0,
  );
}